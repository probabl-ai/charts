apiVersion: v1
kind: Service
metadata:
  name: {{ include "skore-hub-frontend.fullname" . }}
  labels:
    {{- include "skore-hub-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skore-hub-frontend.selectorLabels" . | nindent 4 }}
